mixin tags-list(array, limit)
	//- Data
	if limit
		- array = array.slice(0, limit)
	else
		- array = array
	.tags-list(class!=attributes.class)
		for item in array
			+tag(item)(class="tags-list_item")