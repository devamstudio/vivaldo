extends ../layouts/player-scroller

block prepend head
	-var document_title = ': Albums'

block main
	.container
		// Albums list
		+heading('h2')(class='margin__bottom-0') All albums
		+albums-list(albums)